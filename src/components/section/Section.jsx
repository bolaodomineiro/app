import { SectionContainer } from "./SectionStyles";

const Section = () => {
    return (
        <SectionContainer>
            <h4>waiting for session</h4>
        </SectionContainer>
    )
}

export default Section